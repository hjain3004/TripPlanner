# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
htmlcov/
*.sqlite
*.sqlite3
*.db

# OS / editor
.DS_Store
.idea/
.vscode/

# Node / frontend
node_modules/
.next/
out/
coverage/

# Git worktrees and generated local artifacts
.worktrees/
tmp/
*.log

# Runtime request traces (backend/agents/trace.py, one JSON per request)
backend/.traces/

# Playwright churn (evidence bundles live in frontend/design/refs/)
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/

# Generated MCP configuration (from `shadcn mcp init`)
# We maintain our own opencode.jsonc in the project root instead
opencode.json

# Local env
.env
.env.local
.env.*
!.env.example

# Design brainstorm tooling (durable references live in frontend/design/refs/)
.superpowers/
.playwright-mcp/

# opencode
.opencode/

# Design probe binaries — 11.7MB of derived images/SVG from an exploratory
# session. Sources are Wikimedia CC BY-SA and need attribution before any of
# them are committed. The probe markup itself IS tracked.
frontend/design/probes/*.jpg
frontend/design/probes/*.png
frontend/design/probes/*.svg

# Figma Make export archives — redundant with the extracted bundle under
# design/, which IS tracked as the frozen visual template.
design/*.zip
backend/data/
.firecrawl/
